# Public URL of the deployed Cloudflare Worker. Do not add a trailing slash.
# The TMDB API key belongs only in the Worker's encrypted TMDB_API_KEY secret.
EXPO_PUBLIC_API_BASE_URL=https://kinochain-tmdb-proxy.your-subdomain.workers.dev
